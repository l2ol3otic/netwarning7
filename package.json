{
  "name": "netwarning7",
  "version": "0.1.0",
  "private": true,
  "main": "electron-starter.js",
  "dependencies": {
    "react": "^16.5.0",
    "react-dom": "^16.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "package-win": "electron-packager . electron-tutorial-app --overwrite --platform=win32 --arch=ia32 --icon=src/ioc.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Electron Tutorial App\""
  },
  "devDependencies": {
    "electron": "^2.0.9",
    "electron-packager": "^12.1.2",
    "react-scripts": "1.1.5"
  }
}
